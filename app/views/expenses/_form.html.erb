<%= form_with(model: expense, local: true, class: 'w-full') do |f| %>
  <div class="w-full h-full text-white flex justify-between px-4 py-10 bg-white shadow-lg sm:rounded-3xl sm:px-5 sm:py-6 bg-clip-padding bg-opacity-30 border border-gray-500" style="backdrop-filter: blur(10px);">

  </div>
<% end %>