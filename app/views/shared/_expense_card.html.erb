<div class="w-60 text-white flex justify-between px-4 py-10 bg-white shadow-lg sm:rounded-3xl sm:px-5 sm:py-6 bg-clip-padding bg-opacity-30 border border-gray-500" style="backdrop-filter: blur(10px);">
  <div class="flex flex-col">
    <%= content_tag :p, expense.title %>
    <%= content_tag :p, "#{expense.amount}€", class: expense.amount < 0 ? 'text-red-500' : 'text-green-400' %>
  </div>
  <div style="background-color: <%= expense.category.hex_color%>" class="flex items-center justify-center rounded-xl h-12 w-12">
    <%= expense.category.icon.html_safe %>
  </div>
</div>