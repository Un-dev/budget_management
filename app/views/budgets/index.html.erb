<%= render 'shared/header', title: 'Budgets' %>
<div class='flex flex-col space-y-4 items-center py-3 bg-gray-100 h-full'>
  <% current_user.budgets.includes(:category).each do |budget| %>
    <%= render budget %>
  <% end %>
</div>